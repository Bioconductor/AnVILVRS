% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_caf.R
\name{get_caf}
\alias{get_caf}
\title{Get the allele frequency from 1000 Genomes Project VCF file}
\usage{
get_caf(
  vrs_id,
  vcf,
  vcf_index,
  phenotype = "USA",
  pop_desc_file = NULL,
  toolkit_dir = "./vrs_anvil_toolkit"
)
}
\arguments{
\item{vrs_id}{`character(1)` A GA4GH VRS Allele ID}

\item{vcf}{`character(1)` Path to a bgzipped VCF file containing 1000 Genomes
Project variants with allele frequency annotations.}

\item{vcf_index}{`character(1)` Path to the index file for the VCF file.}

\item{phenotype}{`character(1)` Population or super-population code from the
1000 Genomes Project. See `anvil-datastorage/AnVIL_1000G_PRIMED-data-model`
workspace under `data/population_descriptor.tsv` for valid codes. Defaults
to "USA".}

\item{pop_desc_file}{`character(1)` Path to the population descriptor file
downloaded from the AnVIL_1000G_PRIMED-data-model workspace. If `NULL`
(default), the file will be downloaded to a temporary directory.}

\item{toolkit_dir}{`character(1)` Path to the directory containing the
`vrs_anvil_toolkit/1000g` subdirectory. Defaults to the current working
directory and `vrs_anvil_toolkit` folder.}
}
\description{
Get the allele frequency from 1000 Genomes Project VCF file
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
  library(reticulate)
  ## OR use full path to vrs_env
  use_virtualenv("vrs_env", required = TRUE)
  vcf <- "../vrs_anvil_toolkit/tests/fixtures/1kGP.chr1.1000.vrs.vcf.gz"
  vcf_index <- "../1000g_chr1_index.db"
  variant_id <- "chr1-20094-TAA-T"
  vrs_id <- get_vrs_id(variant_id, "gnomad")
  pop_desc <- get_pop_descriptor()
  get_caf(
    vrs_id, vcf, vcf_index, "USA",
    pop_desc_file = pop_desc, toolkit_dir = "../vrs_anvil_toolkit"
  )
\dontshow{\}) # examplesIf}
}
